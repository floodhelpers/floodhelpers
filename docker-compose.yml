app:
  build: .
  volumes_from:
    - storage
  environment:
    CLEARDB_DATABASE_URL: mysql://b31aa78a5e1afa:c56f3afa@us-cdbr-iron-east-03.cleardb.net/heroku_f043d088b5d163c?reconnect=true

nginx:
  image: nginx
  volumes:
    - ./nginx.conf:/etc/nginx/nginx.conf:ro
  ports:
    - 8080:80
  links:
    - app

storage:
  image: busybox
  command: /bin/true
  volumes:
    - "${STORAGE_BASE}/craft/storage:/app/craft/storage"